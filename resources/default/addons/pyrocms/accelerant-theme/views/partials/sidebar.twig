<aside id="sidebar" class="scrollbar {{ preference_value('pyrocms.theme.accelerant::sidebar_hover', setting_value('pyrocms.theme.accelerant::sidebar_hover')) ? 'hover' }}">

{# {% for key, navigation in template.cp.navigation %}
	{{ dump( navigation.title ) }}
	
{% endfor %} #}

    <ul>
        {% for key, navigation in template.cp.navigation if "navigation" in navigation.title or "pages" in navigation.title %}
        
            
            <li class="{{ navigation.active ? 'active' }} variant-border" data-slug="{{ navigation.slug }}">
                <a {{ html_attributes(navigation.attributes) }} title="{{ trans(navigation.title) }}">
                    <span class="icon">{{ navigation.icon()|raw }}</span>
                    <span class="title">{{ trans(navigation.title) }}</span>
                </a>
            </li>
        {% endfor %}
    </ul>

</aside>
